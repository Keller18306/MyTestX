export const config: {
    serverHost: string,
    serverPort: number,

    bindHost: string,
    bindPort: number
} = {
    serverHost: '127.0.0.1',
    serverPort: 5005,

    bindHost: '127.0.0.1',
    bindPort: 5006
}